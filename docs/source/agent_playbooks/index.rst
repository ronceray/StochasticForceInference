Agent playbooks
===============

Focused, task-oriented guides for AI coding agents (and human
contributors) working on SFI. Each playbook assumes the reader has
already read ``AGENTS.md`` at the repository root and knows the
canonical imports table.

.. toctree::
   :maxdepth: 1

   apply_inference
   add_feature
