# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
/build/
*.egg

# Editor backup/swap files
*~
\#*\#
*.bak
*.swp
*.swo

# Dropbox conflicts
*(conflicted)*

# OS
.DS_Store
Thumbs.db

# IDE / editor settings
.vscode/
.idea/
.local_backup/

# Virtual environments
sfi_env/
.venv/
venv/

# Jupyter
.ipynb_checkpoints/

# pytest
.pytest_cache/

# Coverage
htmlcov/
.coverage

# Sphinx build
docs/build/
docs/source/api/
nohup.out

# Benchmark results (large cached data, run locally) — but keep the small
# pre-rendered ic_comparison report tables that the benchmark docs embed.
benchmark_results/*
!benchmark_results/ic_comparison/
benchmark_results/ic_comparison/*
!benchmark_results/ic_comparison/*.html

# Gallery simulation cache (large npy files, regenerated on demand)
examples/gallery/_cache/

# Generated output directories
examples/manual_examples/*_output/

# Working notes (local only, not user-facing docs)
strang_tests/
ASSESSMENT_strang_sfi_design.md
BENCHMARK_eta_auto_summary.md

# Temp files
temp/
docs_build_errors.log

# Old / legacy
OLD BENCHMARKS/

# Paper drafts (private)
PASTIS_paper.tex

# Dev-only backup files
SFI/inference/sparsity_backup.py

# Project index: ignore the generated bulk, but keep the committed canonical
# API map (regenerated by scripts/gen_api_map.py; referenced by AGENTS.md).
_project_index/*
!_project_index/api_map.json
!_project_index/api_map.md
make_project_index.py

# Slides workspace (local)
slides/

# Experimental raw data (large files, not version-controlled)
examples/experimental_data/raw/

# Local oversized notebook backups (kept local only)
examples/experimental_data/golden2023_analysis.ipynb.bak2
examples/experimental_data/golden2023_analysis.ipynb.bak3

# Scratch notebooks at repo root (anchored: the gallery ships a
# legitimate docs/source/gallery/lorenz_demo.ipynb)
/lorenz_demo.ipynb

# Archived design notes (historical, not version-controlled)
design_notes_history/

# agent/scratch areas that must never be committed
.cache/
.claude/worktrees/

# exploratory, non-package experiment (SFI-for-finance brainstorm)
experiments/finance_sfi/

# scratch / regenerable junk (never committed)
texput.log
xla_gpu_per_fusion_autotune_cache_dir/
_fix_backup_uncommitted/
scripts/_probe_*.py

# demo / experiment outputs at repo root (regenerable)
/F.meta.json
/F.params.npz
/force_model.eqx
/force_model.meta.json
/force_model.params.npz
/my_inference.json
/my_inference.npz
/run.parquet
/my_experiments/
