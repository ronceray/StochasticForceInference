"""Developer utilities and benchmark/calibration scripts for SFI.

These are run directly (``python scripts/<name>.py``) rather than imported as
a library; this package marker simply lets them share helpers when needed.
"""
